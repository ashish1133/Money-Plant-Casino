{
  "name": "money-plant-functions",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": "18" },
  "scripts": {
    "build": "echo 'No build step'",
    "start": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.7.0"
  }
}
